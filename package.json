{
  "name": "rocksoul-correlation",
  "version": "0.4.0",
  "private": false,
  "type": "module",
  "description": "Public provenance-first correlation graph with search, bounded traversal, provenance and freshness APIs for the MoonWitness × Rocksoul ecosystem.",
  "exports": {
    ".": "./src/runtime.mjs",
    "./server": "./src/server.mjs",
    "./freshness": "./src/freshness.mjs"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node src/server.mjs",
    "ci": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
